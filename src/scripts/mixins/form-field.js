export default {
  props: {
    // UI attributes
    noWrap: {
      type: Boolean,
      default: false
    },
    block: {
      type: Boolean,
      default: false
    },
    // Position the input after the label
    alignEnd: {
      type: Boolean,
      default: false
    }
  }
};
